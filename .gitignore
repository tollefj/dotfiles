.DS_Store
*.swp
__pycache__

# Stowed packages must stay tracked; ignore only local-only state.
.config/gh/hosts.yml
*.old

# Self-managed git repos that live in ~ but are NOT part of dotfiles.
# (~/.claude has its own remote: github.com/tollefj/.claude)
.claude/
