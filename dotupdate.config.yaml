# Configuration for dotupdate.py
# Files and directories to ignore when discovering dotfiles in the repository

ignore_items:
  # Git files
  - .git
  - .gitignore
  - .gitconfig

  # dotupdate files
  - dotupdate.py
  - dotupdate.config.yaml

  # Archived files
  - old

  # IDE/Editor configurations
  - .config/github-copilot
